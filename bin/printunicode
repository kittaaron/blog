#!/usr/bin/python
# -*- encoding: utf8 -*-

import sys

def _main():
	args = sys.argv
	argc = len(args)
	if len(args) < 2:
		print "pls enter param"
		exit(0)
	
	str = args[1]
	print repr(str)
	print repr(str.decode("UTF-8"))

if __name__ == "__main__":
	_main()
