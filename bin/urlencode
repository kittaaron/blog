#!/usr/bin/python
# -*- encoding: utf8 -*-

import sys
import urllib

def _main():
	args = sys.argv
	argc = len(args)
	if len(args) < 2:
		print "pls enter param"
		exit(0)
	
	str = args[1]
	print urllib.urlencode(str)

if __name__ == "__main__":
	_main()
